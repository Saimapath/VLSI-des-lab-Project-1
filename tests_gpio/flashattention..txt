{"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.10.0"},"kaggle":{"accelerator":"none","dataSources":[],"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":5,"nbformat":4,"cells":[{"id":"4cd13284-da7f-458d-9309-83c0f8929fcb","cell_type":"markdown","source":"# PA4: Scaled Dot-Product Attention & FlashAttention in CUDA\n\nParts A–C use **float16** tensors. Part D uses a custom CUDA kernel (float32 internally).\n\n> Make sure **GPU is enabled** in Kaggle (Settings → Accelerator → GPU T4 x2 or P100).","metadata":{}},{"id":"bdf3b567-f911-4754-9c64-ba18b295d6f4","cell_type":"code","source":"import torch\nimport torch.nn.functional as F\nimport math, time\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndevice = 'cuda'\nassert torch.cuda.is_available(), 'GPU not available!'\nprint('GPU:', torch.cuda.get_device_name(0))\n\n# ── Fixed hyperparameters ────────────────────────\nB   = 8       # batch size\nNH  = 16      # number of heads\nD   = 64      # head dimension\nSCALE = 1.0 / math.sqrt(D)\n\nN_VALUES  = [32, 64, 128, 256, 512, 1024, 2048, 4096, 8192]\nWARMUP    = 3\nITERS     = 10\nDTYPE     = torch.float16   # Parts A-C\n\ndef make_qkv(N, dtype=DTYPE):\n    \"\"\"Returns Q, K, V of shape [B, NH, N, D] on GPU.\"\"\"\n    return (\n        torch.randn(B, NH, N, D, device=device, dtype=dtype),\n        torch.randn(B, NH, N, D, device=device, dtype=dtype),\n        torch.randn(B, NH, N, D, device=device, dtype=dtype),\n    )\n\ndef time_fn(fn, *args, warmup=WARMUP, iters=ITERS):\n    \"\"\"Returns average runtime in ms over `iters` measured iterations.\"\"\"\n    for _ in range(warmup):\n        fn(*args)\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n    for _ in range(iters):\n        fn(*args)\n    torch.cuda.synchronize()\n    return (time.perf_counter() - t0) / iters * 1e3   # ms\n\nprint('Setup complete.')","metadata":{},"outputs":[],"execution_count":null},{"id":"1d95e742-e1b0-4914-92bc-cbee74a8b0d8","cell_type":"markdown","source":"## Part A: Standard Attention in PyTorch\n\nImplements $O = \\text{softmax}\\!\\left(\\frac{QK^\\top}{\\sqrt{D}}\\right)V$ using `torch.matmul` and `torch.softmax`.\nForms the **full** $N\\times N$ matrix explicitly — no tiling.","metadata":{}},{"id":"d46d19c0-1880-4d6c-8c94-d878c75d11b8","cell_type":"code","source":"def standard_attention(Q, K, V):\n    \"\"\"\n    Q, K, V : [B, NH, N, D]  float16\n    Returns  : [B, NH, N, D]  float16\n    \"\"\"\n    # scores: [B, NH, N, N]\n    scores = torch.matmul(Q, K.transpose(-2, -1)) * SCALE\n    attn   = torch.softmax(scores, dim=-1)          # along key dim\n    return torch.matmul(attn, V)\n\n# Quick smoke-test\nQ, K, V = make_qkv(128)\nout_A   = standard_attention(Q, K, V)\nprint('Part A output shape:', out_A.shape, out_A.dtype)\n\n# ── Timing ──────────────────────────────────────\ntimes_A = []\nfor N in N_VALUES:\n    Q, K, V = make_qkv(N)\n    t = time_fn(standard_attention, Q, K, V)\n    times_A.append(t)\n    print(f'  N={N:5d}  {t:.3f} ms')\n\nprint('Part A timing done.')","metadata":{},"outputs":[],"execution_count":null},{"id":"3e09867e-d964-4fa8-8d25-73b6f3148498","cell_type":"markdown","source":"## Part B: FlashAttention in Python (tile loops)\n\nPython `for`-loops over tiles — **correct, but slow**. Purpose: understand the online-softmax recurrence before porting to CUDA.\n\n**Tile sizes** (from the paper's formula for $D=64$):\n- $M = 32\\times1024$ floats\n- $B_c = M \\;/\\; (4D) = 128$,  $B_r = \\min(B_c, D) = 64$\n\nRunning stats per row: $m_i$ (running max), $\\ell_i$ (running normalizer), $O_i$ (running output).","metadata":{}},{"id":"dbd901f8-3258-45a7-aa6a-cefedae0fb91","cell_type":"code","source":"def flash_attention_python(Q, K, V):\n    \"\"\"\n    FlashAttention with Python for-loops over tiles.\n    Q, K, V : [B, NH, N, D]  float16\n    Returns  : [B, NH, N, D]  float16\n    \"\"\"\n    _B, _NH, N, d = Q.shape\n\n    # Tile sizes from the paper (D=64)\n    M  = 32 * 1024                  # on-chip SRAM budget in floats\n    Bc = M  // (4 * d)              # = 128 for D=64\n    Br = min(Bc, d)                 # = 64  for D=64\n    Tc = (N + Bc - 1) // Bc        # number of K/V tiles\n    Tr = (N + Br - 1) // Br        # number of Q  tiles\n\n    scale = 1.0 / math.sqrt(d)\n\n    # Running buffers  (use float32 internally for stability)\n    O = torch.zeros(_B, _NH, N, d,  device=Q.device, dtype=torch.float32)\n    m = torch.full ((_B, _NH, N),   float('-inf'),    device=Q.device, dtype=torch.float32)\n    l = torch.zeros(_B, _NH, N,                       device=Q.device, dtype=torch.float32)\n\n    Q32 = Q.float();  K32 = K.float();  V32 = V.float()\n\n    # Outer loop: K/V tiles\n    for j in range(Tc):\n        Kj = K32[:, :, j*Bc : (j+1)*Bc, :]   # [B, NH, Bc, d]\n        Vj = V32[:, :, j*Bc : (j+1)*Bc, :]   # [B, NH, Bc, d]\n\n        # Inner loop: Q tiles\n        for i in range(Tr):\n            Qi = Q32[:, :, i*Br : (i+1)*Br, :]   # [B, NH, Br, d]\n\n            # 1. Tile attention scores  [B, NH, Br, Bc]\n            Sij     = torch.matmul(Qi, Kj.transpose(-2, -1)) * scale\n\n            # 2. Tile statistics\n            m_tilde = Sij.amax(dim=-1)                    # [B, NH, Br]\n            P_tilde = torch.exp(Sij - m_tilde.unsqueeze(-1))  # [B, NH, Br, Bc]\n            l_tilde = P_tilde.sum(dim=-1)                 # [B, NH, Br]\n\n            # 3. Online softmax update\n            mi = m[:, :, i*Br : (i+1)*Br]                # [B, NH, Br]\n            li = l[:, :, i*Br : (i+1)*Br]\n            Oi = O[:, :, i*Br : (i+1)*Br, :]             # [B, NH, Br, d]\n\n            m_new = torch.maximum(mi, m_tilde)\n            exp_mi    = torch.exp(mi     - m_new)         # [B, NH, Br]\n            exp_mtile = torch.exp(m_tilde - m_new)\n            l_new = exp_mi * li + exp_mtile * l_tilde\n\n            # O_new = (exp_mi*li*O_old + exp_mtile * P_tilde @ V_j) / l_new\n            PV = torch.matmul(P_tilde, Vj)                # [B, NH, Br, d]\n            O[:, :, i*Br:(i+1)*Br, :] = (\n                exp_mi.unsqueeze(-1)    * li.unsqueeze(-1) * Oi +\n                exp_mtile.unsqueeze(-1) * PV\n            ) / l_new.unsqueeze(-1)\n\n            m[:, :, i*Br:(i+1)*Br]    = m_new\n            l[:, :, i*Br:(i+1)*Br]    = l_new\n\n    return O.to(Q.dtype)\n\n# ── Correctness check vs Part A ─────────────────\ntorch.manual_seed(0)\nQ, K, V = make_qkv(128)\nout_A = standard_attention(Q, K, V)\nout_B = flash_attention_python(Q, K, V)\ndiff  = (out_A.float() - out_B.float()).abs().max().item()\nprint(f'Part B vs A  max |diff| = {diff:.2e}  (must be < 1e-3)')\nassert diff < 1e-3, 'Correctness check FAILED'\n\n# ── Timing (only up to N=512 — Python loops are slow) ──\ntimes_B = []\nN_B     = [N for N in N_VALUES if N <= 512]\nfor N in N_B:\n    Q, K, V = make_qkv(N)\n    t = time_fn(flash_attention_python, Q, K, V, warmup=1, iters=3)\n    times_B.append(t)\n    print(f'  N={N:5d}  {t:.1f} ms')\n\nprint('Part B done.')","metadata":{},"outputs":[],"execution_count":null},{"id":"1059965f-5c1f-4b67-9c50-dc001a948f5a","cell_type":"markdown","source":"## Part C: PyTorch Built-in FlashAttention\n\n`F.scaled_dot_product_attention` uses a fused CUDA kernel (FlashAttention) when available.\nThis is the **performance upper bound**.","metadata":{}},{"id":"f4e6b524-535d-4700-9108-1d48cf6bcd3e","cell_type":"code","source":"def pytorch_flash_attention(Q, K, V):\n    \"\"\"\n    Uses PyTorch's built-in fused attention kernel.\n    Q, K, V : [B, NH, N, D]  float16\n    \"\"\"\n    # scale is applied automatically; no need to pass it explicitly\n    return F.scaled_dot_product_attention(Q, K, V)\n\n# ── Correctness check vs Part A ─────────────────\ntorch.manual_seed(0)\nQ, K, V = make_qkv(128)\nout_A = standard_attention(Q, K, V)\nout_C = pytorch_flash_attention(Q, K, V)\ndiff  = (out_A.float() - out_C.float()).abs().max().item()\nprint(f'Part C vs A  max |diff| = {diff:.2e}  (must be < 1e-3)')\nassert diff < 1e-3, 'Correctness check FAILED'\n\n# ── Timing ──────────────────────────────────────\ntimes_C = []\nfor N in N_VALUES:\n    Q, K, V = make_qkv(N)\n    t = time_fn(pytorch_flash_attention, Q, K, V)\n    times_C.append(t)\n    print(f'  N={N:5d}  {t:.3f} ms')\n\nprint('Part C done.')","metadata":{},"outputs":[],"execution_count":null},{"id":"6a6f892a-42af-4605-afae-b527aa5065b4","cell_type":"markdown","source":"## Part D: FlashAttention Forward Pass — Custom CUDA Kernel\n\n**Fixed tile sizes:** $B_r = B_c = 32$, head dim $d = 64$.\n\n**Grid/Block layout:**\n- `grid = (B, NH)` — each block owns one *(b, h)* slice\n- `block = (Br,)` = 32 threads — each thread owns **one row** of the current Q-tile\n\n**Shared memory** (single `extern __shared__` array, manually partitioned):\n```\nQi [Br × d]   K-tile [Bc × d]   V-tile [Bc × d]   S [Br × Bc]\n```\n\n**Loop order:** outer = j over $T_c$ K/V tiles → inner = i over $T_r$ Q tiles.\n","metadata":{}},{"id":"2e645de4-ca18-4557-b445-6fccd3874a99","cell_type":"code","source":"from torch.utils.cpp_extension import load_inline\n\n# ────────────────────────────────────────────────────────────────────────────\n#  CUDA kernel source\n# ────────────────────────────────────────────────────────────────────────────\nCUDA_SRC = r\"\"\"\n#include <cuda_runtime.h>\n#include <float.h>\n#include <math.h>\n\n// Fixed constants matching the assignment\n#define D_MAX 64\n\n__global__ void flash_attn_fwd_kernel(\n    const float* __restrict__ Q,\n    const float* __restrict__ K,\n    const float* __restrict__ V,\n    float*       __restrict__ O,\n    float*       __restrict__ m_buf,   // [B, NH, N]  running max\n    float*       __restrict__ l_buf,   // [B, NH, N]  running sum\n    int N, int d, int Br, int Bc,\n    int Tr, int Tc,\n    float scale, int NH)\n{\n    // ── Shared memory layout ──────────────────────────────────────────────\n    // Qi : Br x d  |  Kj : Bc x d  |  Vj : Bc x d  |  S : Br x Bc\n    extern __shared__ float smem[];\n    float* Qi = smem;\n    float* Kj = Qi + Br * d;\n    float* Vj = Kj + Bc * d;\n    float* S  = Vj + Bc * d;\n\n    int b  = blockIdx.x;   // batch index\n    int h  = blockIdx.y;   // head  index\n    int tx = threadIdx.x;  // thread index in {0, ..., Br-1}\n\n    // Base offsets into flat [B, NH, N, d] arrays\n    int qkv_base = (b * NH + h) * N * d;\n    int lm_base  = (b * NH + h) * N;\n\n    // ── Outer loop: K/V tiles (j = 0 ... Tc-1) ───────────────────────────\n    for (int j = 0; j < Tc; j++) {\n\n        // --- Cooperative load of K_j and V_j into shared memory ----------\n        // thread tx loads row tx of the tile  (Br == Bc == 32)\n        int kv_row = j * Bc + tx;\n        for (int x = 0; x < d; x++) {\n            float kval = (kv_row < N) ? K[qkv_base + kv_row * d + x] : 0.f;\n            float vval = (kv_row < N) ? V[qkv_base + kv_row * d + x] : 0.f;\n            Kj[tx * d + x] = kval;\n            Vj[tx * d + x] = vval;\n        }\n        __syncthreads();   // BARRIER 1: all Bc rows of K/V are in smem\n\n        // ── Inner loop: Q tiles (i = 0 ... Tr-1) ─────────────────────────\n        for (int i = 0; i < Tr; i++) {\n\n            int q_row  = i * Br + tx;        // global sequence row for this thread\n            bool valid = (q_row < N);\n\n            // Step 1 — Load Q row into shared memory (only tx's slot)\n            if (valid) {\n                for (int x = 0; x < d; x++)\n                    Qi[tx * d + x] = Q[qkv_base + q_row * d + x];\n            }\n            // No barrier needed: only tx ever reads Qi[tx*d + *]\n\n            // Step 2 — Read running statistics from global memory\n            float m_prev = valid ? m_buf[lm_base + q_row] : -FLT_MAX;\n            float l_prev = valid ? l_buf[lm_base + q_row] : 0.f;\n\n            // Step 3 — Compute Bc dot products + tile max\n            int actual_Bc = (j * Bc + Bc <= N) ? Bc : (N - j * Bc);\n            float row_m   = -FLT_MAX;\n            if (valid) {\n                for (int y = 0; y < actual_Bc; y++) {\n                    float dot = 0.f;\n                    for (int x = 0; x < d; x++)\n                        dot += Qi[tx * d + x] * Kj[y * d + x];\n                    dot *= scale;\n                    S[tx * Bc + y] = dot;\n                    row_m = fmaxf(row_m, dot);\n                }\n            }\n\n            // Step 4 — Exponentiate in-place, accumulate tile normalizer\n            float row_l = 0.f;\n            if (valid) {\n                for (int y = 0; y < actual_Bc; y++) {\n                    float p      = expf(S[tx * Bc + y] - row_m);\n                    S[tx * Bc + y] = p;\n                    row_l       += p;\n                }\n            }\n\n            // Step 5 — Online softmax update + output rescaling\n            if (valid) {\n                float m_new       = fmaxf(m_prev, row_m);\n                float exp_m_prev  = expf(m_prev  - m_new);\n                float exp_m_tile  = expf(row_m   - m_new);\n                float l_new       = exp_m_prev * l_prev + exp_m_tile * row_l;\n\n                for (int x = 0; x < d; x++) {\n                    // P*V contribution for embedding index x\n                    float pv = 0.f;\n                    for (int y = 0; y < actual_Bc; y++)\n                        pv += S[tx * Bc + y] * Vj[y * d + x];\n\n                    float o_old = O[qkv_base + q_row * d + x];\n                    // Rescale old accumulator and add new contribution\n                    O[qkv_base + q_row * d + x] =\n                        (exp_m_prev * l_prev * o_old + exp_m_tile * pv) / l_new;\n                }\n\n                m_buf[lm_base + q_row] = m_new;\n                l_buf[lm_base + q_row] = l_new;\n            }\n        } // end inner Q-tile loop\n\n        __syncthreads();   // BARRIER 2: before loading next K/V tile\n\n    } // end outer K/V-tile loop\n}\n\n// ── C++ host wrapper called from Python ──────────────────────────────────\ntorch::Tensor flash_attention_cuda(\n    torch::Tensor Q, torch::Tensor K, torch::Tensor V)\n{\n    // Inputs: [B, NH, N, d]  float32\n    int B_  = Q.size(0);\n    int NH_ = Q.size(1);\n    int N   = Q.size(2);\n    int d   = Q.size(3);\n\n    const int Br = 32, Bc = 32;\n    int Tr    = (N + Br - 1) / Br;\n    int Tc    = (N + Bc - 1) / Bc;\n    float scale = 1.f / sqrtf((float)d);\n\n    auto opts32 = torch::TensorOptions()\n                    .dtype(torch::kFloat32)\n                    .device(Q.device());\n\n    auto O     = torch::zeros({B_, NH_, N, d}, opts32);\n    auto m_buf = torch::full ({B_, NH_, N},    -FLT_MAX, opts32);\n    auto l_buf = torch::zeros({B_, NH_, N},              opts32);\n\n    // Shared memory: Qi + Kj + Vj + S\n    size_t smem = (size_t)(Br*d + 2*Bc*d + Br*Bc) * sizeof(float);\n\n    dim3 grid(B_, NH_);\n    dim3 block(Br);\n\n    flash_attn_fwd_kernel<<<grid, block, smem>>>(\n        Q.data_ptr<float>(), K.data_ptr<float>(), V.data_ptr<float>(),\n        O.data_ptr<float>(),\n        m_buf.data_ptr<float>(), l_buf.data_ptr<float>(),\n        N, d, Br, Bc, Tr, Tc, scale, NH_);\n\n    return O;\n}\n\"\"\"\n\nCPP_SRC = \"torch::Tensor flash_attention_cuda(torch::Tensor Q, torch::Tensor K, torch::Tensor V);\"\n\nflash_ext = load_inline(\n    name='flash_attn_d',\n    cpp_sources=CPP_SRC,\n    cuda_sources=CUDA_SRC,\n    functions=['flash_attention_cuda'],\n    extra_cuda_cflags=['-O2'],\n    verbose=True,\n)\nprint('CUDA kernel compiled successfully.')","metadata":{},"outputs":[],"execution_count":null},{"id":"daaa123f-edf3-4086-9996-d4ddab74713e","cell_type":"code","source":"def flash_attention_D(Q, K, V):\n    \"\"\"\n    Wrapper: accepts float16, converts to float32 for the custom CUDA kernel,\n    returns float16.\n    \"\"\"\n    Q32 = Q.float().contiguous()\n    K32 = K.float().contiguous()\n    V32 = V.float().contiguous()\n    O32 = flash_ext.flash_attention_cuda(Q32, K32, V32)\n    return O32.to(Q.dtype)\n\n# ── Correctness check vs Parts A, B, C ─────────\ntorch.manual_seed(42)\nfor N_test in [32, 64, 128, 256, 512]:\n    Q, K, V = make_qkv(N_test)\n    ref = standard_attention(Q, K, V).float()\n    out = flash_attention_D(Q, K, V).float()\n    diff = (ref - out).abs().max().item()\n    status = 'PASS' if diff < 1e-3 else 'FAIL'\n    print(f'  N={N_test:5d}  max|diff|={diff:.2e}  [{status}]')\n\n# ── Timing ──────────────────────────────────────\ntimes_D = []\nfor N in N_VALUES:\n    Q, K, V = make_qkv(N)\n    t = time_fn(flash_attention_D, Q, K, V)\n    times_D.append(t)\n    print(f'  N={N:5d}  {t:.3f} ms')\n\nprint('Part D done.')","metadata":{},"outputs":[],"execution_count":null},{"id":"99d451ea-49c0-4701-84eb-0eb19c2ef549","cell_type":"markdown","source":"## Plot: Runtime vs. N for all four implementations","metadata":{}},{"id":"ab637802-45dd-4815-8087-0866d2817f76","cell_type":"code","source":"fig, axes = plt.subplots(1, 2, figsize=(14, 5))\n\nfor ax, yscale in zip(axes, ['linear', 'log']):\n    ax.plot(N_VALUES,           times_A,  'o-',  label='A: Standard (PyTorch)',    color='steelblue')\n    ax.plot(N_VALUES,           times_C,  's--', label='C: PyTorch built-in FA',   color='green')\n    ax.plot(N_VALUES,           times_D,  '^-',  label='D: Custom CUDA kernel',    color='darkorange')\n\n    # Part B only available for small N\n    ax.plot(N_B, times_B, 'x:',  label='B: Python tile loops',  color='red')\n\n    ax.set_xlabel('Sequence length N')\n    ax.set_ylabel('Runtime (ms)')\n    ax.set_title(f'Attention runtime vs N  ({yscale} scale)')\n    ax.set_yscale(yscale)\n    ax.set_xscale('log', base=2)\n    ax.set_xticks(N_VALUES)\n    ax.set_xticklabels([str(n) for n in N_VALUES], rotation=45)\n    ax.legend()\n    ax.grid(True, which='both', alpha=0.3)\n\nplt.tight_layout()\nplt.savefig('attention_runtime.png', dpi=150, bbox_inches='tight')\nplt.show()\nprint('Plot saved to attention_runtime.png')","metadata":{},"outputs":[],"execution_count":null},{"id":"00eab0ea-809d-42ba-8647-714f2e7a4012","cell_type":"markdown","source":"## Analysis\n\n### Part C vs Part A\nPyTorch's built-in `scaled_dot_product_attention` (FlashAttention kernel) begins to clearly outperform\nstandard attention (Part A) around **N = 512–1024**. At smaller N the $N^2$ attention matrix fits\ncomfortably in L2/SRAM and the overhead of tiling does not pay off; at larger N the memory\ntraffic for materializing the full $N \\times N$ matrix dominates and FlashAttention's IO-aware\ntiling wins decisively.\n\n### Part B vs Part C\nPart B (Python tile loops) is orders of magnitude slower than Part C even though they execute\nthe same algorithm. The difference comes from:\n1. **Python loop overhead** — every tile iteration pays Python interpreter overhead.\n2. **No kernel fusion** — each `torch.matmul`, `torch.exp`, etc. launches a separate CUDA kernel.\n3. **No shared-memory tiling** — intermediate tiles still live in global GPU memory.\n\nPart C fuses everything into a single kernel that keeps K/V tiles in fast on-chip SRAM,\neliminating global memory round-trips for the $N \\times N$ attention matrix entirely.\n\n### Memory saving from FlashAttention\nStandard attention materializes an $N \\times N \\times B \\times N_H$ matrix → $O(N^2)$ memory.\nFlashAttention never materializes it → $O(N)$ working memory (plus the $O(N \\cdot d)$ output).\nFor $N=8192$, $B=8$, $N_H=16$ the savings are ≈ 65 GB vs ≈ 4 GB.","metadata":{}},{"id":"8a5f158e-ef4b-46b3-8efc-70acd118e61a","cell_type":"code","source":"# ── Summary table ────────────────────────────────\nprint(f'{'N':>6}  {'A (ms)':>10}  {'C (ms)':>10}  {'D (ms)':>10}')\nprint('-' * 45)\nfor i, N in enumerate(N_VALUES):\n    print(f'{N:>6}  {times_A[i]:>10.3f}  {times_C[i]:>10.3f}  {times_D[i]:>10.3f}')","metadata":{},"outputs":[],"execution_count":null}]}